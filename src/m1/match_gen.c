#include <stdio.h>
#include <stdint.h>



int64_t classify(int64_t n) {
  return ({ int64_t _m = n; (0 == _m ? (1) : ; });
}
int main(void) {
  return ({ int64_t _ = println(int_to_string(classify(0))); ({ int64_t _ = println(int_to_string(classify(5))); 0; }); });
}
