world FreezeTest {
    external "m0_println" : String -> Int
    external "m0_int_to_string" : Int -> String

    do main() -> Int {
        set live x : Int = 42;
        set _ : Int = say(int_to_string(x));
        set _ : Int = freeze x;
        say(int_to_string(x))
    }
}
