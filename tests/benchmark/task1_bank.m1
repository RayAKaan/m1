world Bank {
    do main() -> Int {
        set live balance : Int = 1000;
        set _ : Int = now balance >= 0;
        set _ : Int = when balance < 100 then { say("low balance") };
        set _ : Int = balance = balance - 500;
        say(int_to_string(balance))
    }
}
