-- Surface syntax: `say(string-literal)` lowers to m0_println.
-- Prints the literal text followed by a newline. Output: "hi"
world SaySay {
    external "m0_println" : String -> Int

    do main() -> Int {
        set _a : Int = say("hi");
        0
    }
}
